# 작업 원장·브리프 — 산출물이 아니다(저장소에 올리지 않는다)
WORKER_AGORA_TODO.md
.briefs/

# 참가자 로컬 상태가 실수로 들어오는 것을 막는다
*.pem
id_ed25519
id_ed25519.pub
participant.json
ledger.jsonl
config/scrub-names.txt

__pycache__/
*.pyc
.DS_Store

# 외부 리뷰 원문 = 수정 불가 원장·내부 보관 (공개 표현 규약과 영구 충돌하므로 저장소에 두지 않는다)
# ★반영 이력은 사라지지 않는다 — 같은 폴더의 response 문서는 예외로 커밋한다.
.appbuild/review/*
!.appbuild/review/codex-design-r1-response.md
# ★2026-08-26 봉합 라운드 원장 2건은 **예외로 커밋한다** — 실측 금칙어 **0건**이라
#   위 「영구 충돌」 사유가 이 둘에는 성립하지 않는다(구 codex-design-r1.md 는 1건이라 계속 제외).
#   ⚠판단 근거는 파일 성격이 아니라 **측정값**이다: 새 원장을 넣을 때도 먼저 재라.
!.appbuild/review/agy-2026-08-26-agora-final.md
!.appbuild/review/codex-2026-08-26-agora-final.md

# 뮤테이션 저널 — 강제 종료 시 복원용(런타임 산출물)
.agora-mutation-journal
# 동시 실행 거부 잠금(런타임 산출물 · 죽은 pid 면 다음 실행이 회수한다)
.agora-selftest-lock/
