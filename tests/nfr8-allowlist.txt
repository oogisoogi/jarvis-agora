# NFR-8 허용목록 — 정본 경로(docs/·skills/·config/·participants/·.mcp.json·지침 파일)를 대상으로 하는
# 쓰기 sink 중 **그래도 괜찮은 것**. 형식 = `파일:함수  # 왜 괜찮은지`(사유 없는 줄은 없는 것으로 친다).
#
# ★규칙 셋(배선 허용목록과 같다):
#   ⑴ 목록 밖에 마커 sink 가 새로 생기면 적색(새 적재 경로).
#   ⑵ 목록에 있는데 코드에서 사라졌으면 적색(목록이 썩는 것을 막는다).
#   ⑶ 목록에 있어도 그 함수가 이벤트 본문 식별자(body·payload·events·collected·quarantined·stale)를
#      참조하면 적색 — 허용은 「렌더 사본을 쓴다」에 대한 것이지 「남의 글을 쓴다」에 대한 것이 아니다.
#
# ⚠이 검사가 못 잡는 것(숨기지 않는다): 본문이 다른 이름으로 옮겨 담긴 뒤 허용 sink 에 닿는 경로.
#   그래서 이 목록은 짧아야 한다 — 길어지는 순간 검사의 뜻이 사라진다.

agora/brief.py:write_all   # 대리인 브리프 = 우리 렌더(render(role))의 사본을 skills/agora-delegate/ 에 쓴다. 입력은 역할 상수뿐 · 이벤트 본문 0.
