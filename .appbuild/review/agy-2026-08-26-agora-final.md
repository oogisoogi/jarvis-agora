# agy(gemini) 이종 검증 판정 — 설계↔구현 대조 축(TICKET=agora-build MVP 완주 · HEAD e9c1d48) · 2026-08-26
이 원장은 round 번호·STAMPED 를 쓰지 않는다

## ⑴ 재현 계수 표
`bin/agora selftest` 실행 결과 (직접 실측):
- **RC**: 0
- **뮤테이션**: 234/234 KILLED
- **케이스**: 309/309 PASS
- **NOT_APPLIED**: 0건
(기대치 309/309, 234/234, 0과 정확히 일치함)

## ⑵ A 표 15행 대조 결과
05-gate.md Table A의 15행 증거(백틱 그물 이름) 전건을 코드(`agora/selftest.py`)에 grep한 결과:
- **전건(100%) 실재 확인 완료.** 지어낸 이름 없이 모두 실제 테스트 코드로 존재함.

## ⑶ 추적성 재계산 결과
- **기대치**: `04-tasks.md` §2에서 "작업 44건(S1 9 · S2 8 · S3 6 · S4 5 · S5 5 · S6 6 · S7 5) 전부 위 표에 등장한다"고 선언.
- **실측치**: 44건 중 35건만 표에 등장하며, **9건 누락**.
  - 누락된 작업: S1-1, S1-7, S1-8, S2-2, S2-3, S3-4, S4-2, S4-3, S7-5.
  - 결론: 100% 등장 주장은 거짓(명세 불일치). ⑷ 문제점에 기재.

## ⑷ [문제점]
1. **HIGH | `04-tasks.md` §2 추적성 표 거짓 선언 (요구-작업 매핑 누락)**
   - **위치**: `04-tasks.md` §2 (줄 189)
   - **재현**: 파일 내 `역방향 — 작업 44건(...) 전부 위 표에 등장한다` 문구 확인 후, 표 내용을 검산. S1-1 등 9개 작업이 표에 기재되지 않음.
   - **내용**: 문서가 스스로 증명했다고 주장하는 커버리지가 실제 문서 내용과 일치하지 않음.

2. **MEDIUM | 범위 밖 기능의 과잉 구현 (FR-11 검색 필터)**
   - **위치**: `04-tasks.md` §5 (줄 231) 및 `05-gate.md` FR-11 vs `agora/tools.py` `_matches` 함수
   - **재현**: `04`와 `05` 문서 모두 FR-11 검색 구현을 "계약 인자만", "구현은 v0.1 이월"로 선언했으나, `agora/tools.py`의 `_matches` 함수가 실제로 tag, os, app, answered, query 필터링 로직을 전건 수행하고 있음.
   - **내용**: 명시된 "하지 않는 것"을 위반하여 로직이 구현됨(범위 밖인데 구현).

3. **MEDIUM | `03-architecture.md`에 L-1 교착 방어 로직 누락 (옛 서술 유지)**
   - **위치**: `03-architecture.md` §2-3 (줄 73), §6 (줄 150) vs `agora/reducer.py` `reject` 함수
   - **재현**: 성찰 문서 I-2와 `THREAT-MODEL.md` R-10, 실제 코드(`state["head"] = entry["hash"]`)는 "거부 이벤트도 머리(head)를 전진시킨다"고 확정함. 그러나 정본 설계 문서 `03-architecture.md`는 여전히 "reducer 입력 = 유효 이벤트만"이라는 옛 서술을 유지.
   - **내용**: 성찰/위협모델의 내용이 주 설계 문서에 반영되지 않아 아키텍처 원칙과 실제 구현(상태 전이) 간 충돌 발생.

4. **MEDIUM | `04-tasks.md` §0 미해소 항목(F-18) 상태 불일치**
   - **위치**: `04-tasks.md` §0-2 (줄 53) vs `05-gate.md` E 표 (줄 100)
   - **재현**: `05-gate.md`에는 F-18(01-prd 금칙어 위반)이 `✅해소`되어 커밋 통과로 적혀 있으나, `04-tasks.md`에는 여전히 `미해소 1건(HIGH)` 및 `막힘으로 상신`으로 남아 있음. 문서 간 상태 동기화 누락.

## ⑸ [논쟁점]
- **대체 커버의 충족 승격 (C-7 이종 클라이언트 2종)**: 성찰 문서(`reflection-implementation-2026-08-26.md` §5)는 C-7을 "미실행. 대체는 충족이 아니다"라고 썼으나, `05-gate.md`는 "master 실측(02:5x)"을 근거로 `충족` 판정함. 이는 워커의 성찰 작성 이후 master가 직접 개입해 실측하고 게이트를 통과시킨 정상적인 협업 흐름으로 보이므로 결함으로 세지 않음. 
- **운영 동작 3종 노출 위치**: `03-architecture.md` §4의 CLI 전용 동작 3종(`mcp-serve`, `delegate-chair`, `abort`)은 `selftest`의 `local` 및 `cli.py` 명령어 목록과 정확히 일치함. 도구 11종 역시 동일하게 매핑됨.
- **C-6/C-7 층 표기 검증**: 누가(master), 언제(2026-08-26), 무엇으로(초판 A안 / codex-cli, Claude Code) 했는지 05-gate 문서에 투명하게 명시되어 있어 검증 가능성이 높음.

## ⑹ [다음 단계 조언]
1. `04-tasks.md` §2 추적성 표를 수정하여 누락된 9건의 작업을 정확히 매핑하거나, "전부 등장한다"는 문구를 사실에 맞게 정정하라.
2. `03-architecture.md` §2-3 및 §6 문면을 갱신하여, 거부된 이벤트도 사슬 머리를 전진시켜 `state_hash`를 변경한다는 (L-1 봉합) 사실을 정본에 반영하라.
3. FR-11(검색 필터)은 구현을 폐기(v0.1 이월)할지, 명세를 수정하여 현재 구현을 MVP 범위로 편입시킬지 결단이 필요하다.

VERDICT: REVISE (HIGH 1, MEDIUM 3)
