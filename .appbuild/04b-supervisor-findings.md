# 감독관 13항목 검증 보고 — Jarvis Agora (2026-08-25 · 워커 수행)

대상 = `01-prd.md` **v1.1** · `02-screens.md` **v1.0** · `03-architecture.md` **v1.1** · `04-tasks.md` **v2.0**
방법 = 13항목 체크리스트(일관성 4 · 추적성 3 · 갭 4 · 품질 2). 기계로 잴 수 있는 항목은 **명령 출력**으로 재고, 그 외는 전수 대조.

## 요약

| 심각도 | 건수 | 빌드 영향 |
|---|---|---|
| CRITICAL | **0** | — |
| HIGH | **3** | 게이트 **PASS 불가** — 구현 착수는 막지 않음(전부 문서·환경 축) |
| MED | **5** | 구현 자리에 영향 · 잠정안으로 진행하되 한 파일에 격리 |
| LOW | **9** | 문안 교정 |

**커버리지 = 23/23 요구(FR 15 + NFR 8) = 100%** — 04 §2 추적표 기준, 요구 하나마다 ≥1 작업.
**고아 작업 0** — 작업 42건 중 36건이 요구에 직접 추적, 6건(커밋·상신)은 브리프 절차에 추적.

⚠ 이 보고의 심각도는 **내 판정**이다. 문서 01·02·03 의 소유권은 조율자에게 있으므로 **어느 항목도 내가 고치지 않았다.**

---

## 실측 대조 (명령 출력이 근거)

| 대조 | 명령 | 결과 |
|---|---|---|
| MCP 도구 수 | `grep -oE 'agora\.[a-z_]+' 03 \| sort -u` | **11종** (threads·read·propose·say·advance·resolve·mark_solved·close·vote·envelope_check·ack) |
| 이벤트 kind 수 | 03 §2-2 표 행 계수 | **9종** — §9 본문은 「8종」이라 적혀 있다(불일치) |
| §8 검증 행 | 03 §8 표 행 전수 | FR-1/2·3/6/12·4·5·7·8/15·9·10·11·13·14 · NFR-1/3·4·5/6 → **NFR-7·NFR-8 행 없음** |
| 04 추적표 요구 | 04 §2 표 행 전수 | FR-1~15 · NFR-1~8 = **23행 전건** |
| 작업 계수 | 04 작업 id 전수 | **42건**(S1 9·S2 7·S3 6·S4 5·S5 5·S6 5·S7 5) |
| 미완 표식 | 세 표식(`TO`+`DO` · 물음표 3개 · `TB`+`D`)을 `grep -rnE` 로 전수 — **검사 문자열을 이 표에 리터럴로 적지 않는다**(자기 걸림 방지) | **0건** |
| 02 표면↔요구 | 02 §1 대응 FR 열 전수 | S-1 FR-1·11 / S-2 FR-3·6 / S-3 FR-2·7·10 / S-4 FR-8·15 / S-5 FR-2 / S-6 FR-14 / S-7 전 FR → **FR-4·5·9·12·13 미등장** |

---

## 항목별 결과

### 일관성

**① 고아 기능 없음** — ⚠ 위반 2건
- **F-01 · HIGH** — **FR-4(토론 절차)·FR-5(해결 표시)가 02-screens 표면 표에 없다.** v1.1 이 도구를 8종→11종으로 늘리면서 `advance`·`resolve`·`mark_solved`·`close`·`vote`·`ack` 의 **결과 표면이 명세되지 않은 채** 남았다. S-3 은 `propose/say` 만 덮는다. 근거 = 위 실측 표 마지막 행.
- **F-02 · LOW** — FR-9(대리인 절차)·FR-12(온보딩)·FR-13(도구 계약)은 화면이 없는 것이 **정상**이지만 `[NON-GOAL for screens]` 표기가 없어 「누락」과 구별되지 않는다.

**② 모델 외 데이터 참조 없음** — ⚠ 위반 3건(전부 「모델에 없는 것을 참조」가 아니라 **「참조되는데 모델이 없다」**)
- **F-03 · MED** — `participant.json` 이 §7 에 파일로만 있고 **스키마가 없다.** 04 K-5 로 잠정안 제시.
- **F-04 · MED** — `abort` 는 「운영자 키」, 체크포인트는 「운영자 키로 서명」인데 **명부에서 운영자를 식별하는 자리가 없다.** 04 K-3.
- **F-05 · LOW** — `genesis` 의 `prev`·`expected_state` 값이 미정의(공통 필드는 전 이벤트 필수). 04 K-4.

**③ 부모 없는 작업·미정의 컴포넌트 없음** — ⚠ 1건
- **F-06 · LOW** — 04 가 `agora/contract_open.py`(미회신 공백을 상수로 격리하는 파일) 1개를 신설할 예정인데 **03 §7 저장소 구조에 없다.** 회신이 오면 삭제되는 임시 파일이므로 구조에 넣지 않는 편이 맞을 수도 있다 — 조율자 판단 요청.

**④ 용어 드리프트 없음** — ⚠ 3건
- **F-07 · MED** — **같은 사건에 이름이 셋이다**: FR-5 「해결 표시」 · 도구 `agora.mark_solved` · 이벤트 kind `answer_selected`. 도구가 그 kind 를 낸다는 서술이 **어디에도 없어** 읽는 사람이 둘을 다른 기능으로 읽을 수 있다.
- **F-08 · LOW** — 「대표 워커」(01 §4) ↔ 「대리인」·「대표 에이전트」(03·04·스킬명). 저장소 공개 문서에서 하나로 통일 필요.
- **F-09 · LOW** — 01 제목은 **v1.1** 인데 3행 「상태」 줄은 **「v1.0 확정」** 그대로다. 요약 칸이 상세 개정을 못 따라간 형태.

### 추적성

**⑤ 순방향 체인 완결** — ⚠ F-01 로 FR-4·FR-5 의 「요구 → 화면」 칸이 끊긴다. 나머지 21개 요구는 요구 → (화면) → 데이터 → 작업 → AC 까지 완결.

**⑥ 역방향 고아 0 · 커버리지** — 고아 작업 **0** · 커버리지 **100%(23/23)**. ⚠ 단 검증 표 쪽에 구멍 2건:
- **F-10 · MED** — 03 §8 에 **NFR-7·NFR-8 검증 행이 없다.** FR-10 행이 예산을 재지만 NFR-7 은 「토큰 비용」이라 축이 다르고, NFR-8(정본 자동 적재 금지)은 어디서도 재지 않는다. 04 K-6 으로 행 2개 신설 제안.
- **F-11 · LOW** — 03 §8 의 `FR-3/6/12` 행은 내용이 전부 canonical·서명·replay 라 **FR-12(온보딩)를 재지 않는다.** 04 K-8 로 검증 재정의 제안(S7-1 이 수행).

**⑦ ID 안정·유일·연속·참조 해소** — FR-1~15 · NFR-1~8 · S-1~7 · 작업 id 전부 연속·유일·전방 점프 0. ⚠ 1건:
- **F-12 · LOW** — 03 §9 「kind 8종」 ↔ §2-2 표 **9종**. 표가 정본.

### 갭

**⑧ 비-해피 상태 명세** — ⚠ 1건
- **F-13 · HIGH** — **02-screens 의 상태 열이 v1.0 오류 코드(2·3·4·7)만 반영한다.** v1.1 이 신설한 **5(권한)·8(unknown_commit)·9(CAS 불일치)·10(인자 오류)** 과, `read --audit` 의 **격리 이벤트 표시 상태**, `human_approval` 의 **승인 대기·거부 상태**가 표면 명세에 없다. 「비-해피를 다 적었다」가 지금은 참이 아니다.

**⑨ 인증/권한 범위 명시** — ✅ 03 §5·§6 이 의장·요청자·운영자·수신 워커 경계를 명시. ⚠ 단 F-04(운영자 식별 자리)가 그 경계의 한 칸을 비운다.

**⑩ 입력 검증·엣지 케이스** — ✅ 03 §5 allowlist(문자·길이·구조) + denylist + 4KB 상한 + 64KB 상한. ⚠ 1건:
- **F-14 · MED** — **`human_approval` 기본 on 의 비대화형 동작이 미정의.** 이 제품의 목적은 「사람 개입 없이 해결」(G1)인데 모든 발신이 승인 프롬프트를 거치면 무인 운용과 충돌한다.
  · ✅ **확인한 것**: 01 §13 이 성공 지표의 「개입」 정의에서 **승인 프롬프트를 명시적으로 제외**한다(v1.1 신설). 따라서 **요구 충돌은 아니다** — 내가 충돌이라고 단정하기 전에 §13 을 읽어 확인했다.
  · ⚠ **남는 것**: 승인 프롬프트를 **띄울 수 없는 환경**(예약 실행·무인 세션)에서의 동작이 안 적혀 있다. 04 S3-4 는 「**전송하지 않고 code 3**」을 잠정안으로 뒀다 — 조용한 자동 승인보다 안전한 쪽으로 기울인 판단이며, 조율자 확인이 필요하다.

**⑪ 비기능은 형용사 아닌 수치** — ✅ 대체로 충족(4KB·64KB·60초·128비트·5000/h·OpenSSH≥8.2·python≥3.11·gh≥2.40 · 예산 수치). placeholder 0건. ⚠ 1건:
- **F-15 · LOW** — NFR-5(오프라인 내성)에 **보존 기간 수치가 없다.** 운반층 쪽은 저장소가 보존하지만 **로컬 spool·events 의 보존·정리 기준**이 없어 무한 증가한다.

### 품질

**⑫ 작업이 INVEST·수직 슬라이스** — ✅ 04 §4 자기점검 충족. 수평 레이어 슬라이스 0건. 슬라이스마다 「끝나면 무엇이 실제로 되는가」 한 문장 존재.

**⑬ 모든 요구가 테스트 가능·수용 기준 보유** — ⚠ 2건
- **F-17 · HIGH** — **NFR-4 의 「Windows smoke 1회」를 실행할 수단이 이 환경에 없다.** 게이트에 그대로 넣으면 **충족 불가능한 행**이 된다. 04 K-1 권고 = 게이트 행에 「미실행 — 실행 수단 부재」로 **남겨 두고**(지우지 않고) v0.1 이월. 지우면 다음 사람이 「검증했다」로 읽는다.
- **F-16 · LOW** — FR-14 「URL 열면 스레드 원문 가독」이 주관적이다. 「스레드 URL 이 200 을 반환하고 게시물 N건이 화면에 나타난다」처럼 조작적 정의로 바꿀 것을 권고.

---

## 조율자 판단이 필요한 것 (요약 · 우선순위 순)

| # | 무엇 | 미결 시 귀결 |
|---|---|---|
| F-01·F-13 | **02-screens 를 v1.1 로 갱신**(도구 11종 표면 · 오류코드 5·8·9·10 · audit·승인 대기 상태) | 게이트 D(교차정합)가 상시 미충족 · 「비-해피 다 적었다」가 거짓 |
| F-17 | Windows smoke 처리 방침(04 K-1) | 게이트에 충족 불가 행이 남는다 |
| F-07 | `mark_solved` ↔ `answer_selected` 이름 대응 명시 | 두 기능으로 오독 |
| F-04·F-03·F-05 | 운영자 식별 자리 · participant.json 스키마 · genesis prev 값(04 K-3·K-5·K-4) | S1-4·S2-5 착수 시 임의 결정이 굳는다 |
| F-10·F-11·F-12 | §8 에 NFR-7·NFR-8 행 신설 · FR-12 검증 재정의 · kind 계수 교정 | 추적성 표가 닫히지 않는다 |
| F-06 | 임시 파일 1개(`contract_open.py`)를 §7 에 넣을지 | 구조 문서와 실물 불일치 |
| F-14 | 비대화형 human_approval 동작 확정 | 무인 경로에서 임의 판단 |
| F-09·F-08·F-15·F-16 | 문안 교정 4건 | 읽는 사람이 옛 상태를 읽는다 |

---

# 재검증 (2026-08-25 09:5x 조율자 확정 이후 · append)

> 앞 절은 **고치지 않는다**(그때 무엇이 있었는지가 바뀌면 원장이 아니다). 변화는 여기 append 한다.

## 실측 재대조

| 대조 | 결과 |
|---|---|
| 02-screens 표면 수 | **15**(S-1~S-15) — 초판 7 → 도구 11종 + watch·selftest·GitHub·keygen/export |
| 02-screens 오류 코드 등장 | 2·3·4·5·7·8·9·10 **전건 등장**(계수 2:1 · 3:2 · 4:1 · 5:4 · 7:6 · 8:4 · 9:4 · 10:8) |
| 03 §9 kind 계수 | `kind 8종` **0건** · `kind 9종(§2-2)` 로 교정 확인 |
| 03 §8 신설 행 | NFR-7 · NFR-8 · FR-12 **3행 실재** · NFR-4 행에 F-17 A안 박힘 |
| 03 §2-1a | 신설 확인 — K-2·K-3·K-4·K-5·F-14 **5건 확정값 실재** |
| 01 상태 줄 | `v1.1 확정` 으로 교정 확인 |
| 미완 표식 | `.appbuild/*.md` **0건**(F-19 리터럴 분리 후 재측정) |
| 금칙어 | 02·03·04·05·04b = **0건** · **01-prd = 25건** ← F-18 |

## 상태 변화

- **해소 = 16건** (F-01·F-02·F-03·F-04·F-05·F-06·F-07·F-08·F-09·F-10·F-11·F-12·F-13·F-14·F-16·F-17)
- **미해소 = 1건** (F-15 · LOW · spool·events 보존·정리 기준 없음 — 로컬 저장이 무한 증가한다)
- **신규 = 2건**
  - **F-18 · HIGH** — `01-prd.md` 금칙어 25건. **코드가 아니라 커밋을 막는다.** 후보·권고는 04 §0-2.
    · ⚠**이것이 초판 검증에서 안 잡힌 이유를 적어 둔다**: 초판은 13항목(일관성·추적성·갭·품질)만 돌렸고 **저장소 규약 축을 안 봤다.**
      검사 축을 항목 목록으로만 훑으면 목록 밖 축은 영원히 안 보인다 — 이번엔 **커밋 전 게이트를 실제로 돌려 보다가** 나왔다.
  - **F-19 · LOW(해소)** — 미완 표식 검사기가 자기 문서에 걸려 상시 1건이었다. **파일 면제가 아니라 리터럴 분리**로 풀었다.

## 이 재검증이 **안 댄 축** (범위 밖 전수 공개)

- **03 v1.1 전문을 다시 13항목으로 돌리지 않았다.** 이번 재검증은 「초판 17건이 실제로 닫혔는가」 + 「커밋 게이트가 실제로 도는가」 두 축만 봤다.
  v1.1 이 §2-1a·§8 3행을 새로 넣었으므로 **그 신설분에 대한 13항목 재검증은 미실행**이다.
- **reducer 성능**(이벤트 수천 건에서 매번 전량 재계산) — 안 봤다.
- **01 §13 성공 지표**를 게이트가 재지 않는다 — 운영 축이라 의도적으로 뺐다.

## 추가 (재검증 직후 · append)

- **F-20 · MED(즉시 해소)** — **결함을 보고하면서 그 결함을 재생산했다.**
  F-18(금칙어 위반)을 보고할 때 종류별 계수를 적으면서 **그 낱말들을 그대로 04-tasks·05-gate 에 옮겨 적었고**,
  그 순간 두 파일이 0건 → 1건이 됐다(실측으로 발견). F-19 와 같은 계열이고, 우리가 이미 아는 형태다 —
  **봉합 라운드는 자기가 정리하는 병을 새로 만든다.**
  · 처방 = 계수만 적고 **낱말은 적지 않는다**(실물은 명령을 돌려 본다). 재측정: 04-tasks 0건 · 05-gate 0건.
  · ★교훈: **검사 대상 문자열을 인용하는 보고서는 그 검사의 대상이 된다.** 보고 서식에 「대상 문자열을 옮겨 적지 않는다」를 규칙으로 둔다.
