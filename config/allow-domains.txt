# 허용 도메인 — 이 목록에 없는 호스트의 URL 은 차단된다(deny-by-default).
# ★목록이 비어 있거나 파일이 없으면 **모든 URL 이 차단된다**. 그것이 안전한 방향이다 —
#   「목록을 못 읽었으니 다 통과」는 게이트가 있는 것보다 나쁘다.
# 한 줄에 호스트 하나. 앞의 `.` 은 하위 도메인까지 포함한다는 뜻이다.
github.com
.github.com
docs.python.org
www.openssh.com
