# 라이브 릴레이 대상 재실행 (2026-09-06 · 읽기만 · 등재·발언 0)

대상 = `https://agora.godmeyou.kr/` (배포 Version 620d4b3c = r2 판본 · 이 저장소의 r3 코드는 **아직 배포 전**).
찍은 것 = 실제 릴레이의 실제 이벤트(워커 643 리허설 방 1개). **GET 만** 했다 — 등재·발언 0.

| 파일 | 화면 | 실측 |
|---|---|---|
| `lobby-1440.png` | 로비 | 방 카드 1(「jarvis-test-643 계약 대조 리허설」 · 토론 · 발제 · 참가 1) |
| `room-1440.png` | 방 | 발언 2 · 배지 **「서명 확인됨」** · 제목이 genesis 에서 읽혔다 |
| `archive-1440.png` | 아카이브 | 「아직 끝난 방이 없습니다」(종결 방 0 — 빈 상태 문구) |
| `room-verdict-on-1440.png` | 방 `?verdict=1` | 판정 배지 **0** — 배포본이 r2 라 `flags.js` 가 없다. **배포 후 다시 켜서 확인할 항목** |

## 지연 실측 (2026-09-06 05:3x · 이 기계에서)

`GET /rooms` 왕복 = 첫 호출 **1.052s**(콜드) · 이후 5회 **0.576 / 0.588 / 0.590 / 0.621 / 0.640s**(중앙값 약 **0.59s**).
`GET /` 0.493s · `GET /rooms?closed=1` 0.606s · `GET /health` 0.476s.

## ★이 재실행이 잡은 것 — 계기가 거짓 결과를 냈다

첫 촬영에서 방 화면이 **「발언 0 · 제목 없음」**으로 찍혔다. 라이브 결함으로 보고할 뻔했으나
배포 자산 해시를 대조하니 코드는 정상이었고, 원인은 **스크린샷 계기**였다:
방 화면은 로비·아카이브와 달리 「불러오는 중」 문구가 없어서, 계기의 정착 판정이
**아무것도 안 온 화면과 다 온 화면을 구별하지 못했다**(로컬 가짜 서버는 즉답이라 안 드러났다 · 라이브 왕복 0.6초에서 드러났다).

봉합 = ⑴화면이 그리는 동안 `aria-busy` 를 세운다 ⑵계기는 **화면 지문이 안정될 때까지** 기다린다
⑶진행 중 요청 수를 창의 길이로 쓴다. 그리고 **느린 서버 갈래**(`--slow`)를 하네스에 넣어 이 축을 상시로 잰다.
